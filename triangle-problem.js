// var num_rows = 7;
    // for(var row = 0; row < num_rows; row++) {
    //     var toPrint = "#";

    //     for(var column = 0; column < row; column++) {
    //         toPrint += "#";
    //     }
    //     console.log(toPrint);
    // }

        //Second way to create Triangle

    // for (let triangle = "#"; triangle.length <= 7; triangle += "#")
    // console.log(triangle);